/** The relay wsscope points at when you don't name one. */
export const DEFAULT_RELAY = 'wss://vcmc.communities.buzz.xyz'

/** How many stored events to replay when no --limit is given. */
export const DEFAULT_LIMIT = 25
